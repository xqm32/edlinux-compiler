{
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "format": "prettier --write ."
  },
  "dependencies": {
    "hono": "^4.2.2"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3.2.5"
  }
}
